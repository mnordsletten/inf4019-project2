#!/usr/bin/env bash

cd /docker_distribute
git pull origin master > /dev/null 2>&1

cat /docker_distribute/output.txt | grep -q '^test2_status="PASS"'
if [ $? -eq 0 ]; then
        #echo 'test2_status="PASS"'
	exit 0
else
        #echo 'test2_status="FAIL"'
	exit 1
fi
